input {
  tcp {
    port => 5044
    codec => json
  }
}
filter {
  # Add filters as needed
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logs-%{+YYYY.MM.dd}"
  }
}